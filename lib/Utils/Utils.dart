import 'package:flutter/material.dart';

class Utils {
  verticalSpace(double height) {
    return SizedBox(
      height: height,
    );
  }

  horizontalSpace(double width) {
    return SizedBox(
      width: width,
    );
  }
}
